































/*
class DG_duplex {
	public:
		Domain* domain;
		double terminal;
		double salt;
		double initial;
		double dS;
		double dH;

		void set_domain(Domain& domain_){
			domain = &domain_;
		}
		void calc_terminal(){
			terminal = ( ( dH_termAT - T*dS_termAT ) + ( dH_termCG - T*dS_termCG ) ); // 2 * 1/2
		}
		void calc_salt(){
		   	salt = (domain->length-1) * 0.368 * log( 0.5*conc_Tris + 3.3*sqrt(conc_Mg) );
		}
		void calc_dH(){
			dH = (domain->length-1) * dH_average;
		}
		void calc_dS(){
			dS = (domain->length-1) * dS_average;
		}
*/




